package com.nom.projet;

public class Main {
    public static void main(String[] args) {
        /*
        User newUser = new User(
                "Mathieu",
                "Mithridate",
                "test@test.com" ,
                "1234"
        );

        boolean exist = UserRepository.isExist("mathieu1@gmail.com");
        if(exist) {
            System.out.println("Le compte existe");
        }
        else {
            System.out.println("Le compte n'existe pas");
        }

        //FindByEmail
        User getUser = UserRepository.findByEmail("soler.marjorie@exemple.com");
        if(getUser != null){
            System.out.println(getUser);
        }else{
            System.out.println("Le compte n'existe pas");
        }

        //findAll
        User getUsers = UserRepository.findAll();
        if(getUsers != null){
            System.out.println(getUsers);
        }else{
            System.out.println("Aucun utilisateur trouvé");
        }


        //Update
        // Ancien email de l'utilisateur à modifier
        String oldEmail = "soler.marjo81@exemple.com";

        // Nouvel objet User avec les nouvelles informations
        User updatedUser = new User("Jonathan", "Doe", "soler.marjorie81@exemple.com");

        // Appel de la méthode update
        User modifiedUser = UserRepository.update(updatedUser, oldEmail);

        // Vérifier et afficher le résultat
        if (modifiedUser != null) {
            System.out.println("Mise à jour réussie : " + modifiedUser);
        } else {
            System.out.println("Échec de la mise à jour : utilisateur non trouvé.");
        }
        */


    }
}
